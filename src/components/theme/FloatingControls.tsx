import { FloatingThemeToggle } from './FloatingThemeToggle';
import { FloatingAccentSelector } from './FloatingAccentSelector';

export function FloatingControls() {
  return (
    <>
      <FloatingThemeToggle />
      <FloatingAccentSelector />
    </>
  );
}